name: conditioned
description: This package contains a Widget that will let you show a child Widget based on provided conditions.
version: 0.1.0-dev.1
homepage: https://github.com/SEGVeenstra/conditioned

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
