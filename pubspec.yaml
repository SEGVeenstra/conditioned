name: condition
description: This package contains a Widget that will let you show a child Widget based on provided conditions.
version: 0.0.8
homepage: https://github.com/SEGVeenstra/flutter-package-condition

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
